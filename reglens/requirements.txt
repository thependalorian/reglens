# Profile B — LangGraph 1.x + Pydantic AI (Ketchup DEV stack)
pydantic==2.13.4
pydantic-ai==0.3.5
pydantic-ai-slim==0.3.5
pydantic-settings>=2.8

langgraph==1.2.9
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-checkpoint-postgres==3.1.0
langgraph-sdk==0.4.2

# AG-UI protocol bridge for the CopilotKit frontend (/agui endpoint).
# 0.0.42 fixes the interrupt-as-state emission and the #706 regenerate
# detection natively (needs langgraph>=1 / langchain>=1.x), which lets the
# HITL panel render on the supported path without the GraphInterrupt hacks.
ag-ui-langgraph==0.0.42
langchain==1.3.14
langchain-core==1.5.0

# copilotkit SDK — the /agui endpoint serves copilotkit's LangGraphAGUIAgent
# (subclass of ag_ui_langgraph's LangGraphAgent) and the state declares the
# copilotkit actions/context channel. Pulls the langgraph 1.x line above.
copilotkit==0.1.94

fastapi>=0.115.12
uvicorn>=0.34
httpx==0.28.1
openai>=1.90
python-dotenv>=1.0
python-multipart>=0.0.20
asyncpg==0.30.0

aiohttp>=3.9
langfuse>=3.2
pgvector>=0.3.6

pytest>=8.4
pytest-asyncio>=1.0

# ============================================================
# Document Parsing
# ============================================================

# Docling — RECOMMENDED local parser.
# AI/ML layout understanding, preserves table structure, handles
# multi-column layouts, no API key required. Runs fully locally —
# no corpus data leaves the regulator's environment.
# Handles: PDF, DOCX, PPTX, XLSX, HTML.
# Critical for: FATF reports, Basel documents, SADC/BoN circulars.
docling==2.113.0

# LlamaParse — OPTIONAL cloud parser (best quality).
# Requires LLAMA_CLOUD_API_KEY (10k pages/month free).
# Uncomment to enable:
# llama-parse==0.6.94
# llama-index-core==0.14.23
